apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  labels:
    app: frontend
spec:
  selector:
    app: frontend
  ports:
    - port: 80           # Cluster IP port
      targetPort: 80     # Container port
  type: ClusterIP        # Change to LoadBalancer/NodePort for external access
